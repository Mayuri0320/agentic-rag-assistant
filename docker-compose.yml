services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: agentic-rag-api

    ports:
      - "8000:8000"

    volumes:
      - .:/app
      - /app/.venv

    restart: unless-stopped